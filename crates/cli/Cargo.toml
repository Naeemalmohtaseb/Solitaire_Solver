[package]
name = "solitaire-cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
clap.workspace = true
solver_core = { path = "../solver_core" }

[[bin]]
name = "solitaire-cli"
path = "src/main.rs"

