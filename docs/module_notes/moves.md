# moves

Owns atomic moves, planner macro moves, legal move generation, apply/undo, auto-flip side effects, and semantic move tags.

Future implementation must be exact before search layers depend on it.

