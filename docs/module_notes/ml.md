# ml

Owns later machine-learning boundaries.

The first neural layer should be a deterministic full-state V-Net. P-Net belief priors are optional later and should not precede a strong non-neural engine.

