# planner

Owns the event-driven belief-state sparse UCT planner.

Future work will implement decision nodes, deterministic corridor nodes, reveal chance nodes, root action statistics, branch-priority budgeting, and confidence-based stopping.

